# Руководство по внесению вклада в AggreGate SDK

Спасибо за интерес к проекту AggreGate SDK! Мы приветствуем вклад от сообщества.

## Как внести вклад

### Сообщения об ошибках

Если вы нашли ошибку:

1. Проверьте, не была ли она уже зарегистрирована в [Issues](https://github.com/Michaael/AggreGate-SDK-CE)
2. Если нет, создайте новую issue с:
   - Четким описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым и фактическим поведением
   - Версией Java, Gradle и операционной системы
   - Логами ошибок (если применимо)

### Предложения по улучшению

Для предложений новых функций:

1. Создайте issue с меткой "enhancement"
2. Опишите предлагаемую функцию
3. Объясните, почему это будет полезно
4. Предложите возможную реализацию (если есть идеи)

### Pull Requests

1. **Форкните репозиторий**
2. **Создайте ветку** для вашей функции:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. **Следуйте стилю кода**:
   - Используйте Java 8+ features
   - Избегайте raw types
   - Добавляйте JavaDoc для публичных API
   - Пишите тесты для нового кода
4. **Убедитесь, что тесты проходят**:
   ```bash
   ./gradlew test
   ```
5. **Закоммитьте изменения**:
   ```bash
   git commit -m "Add amazing feature"
   ```
6. **Отправьте в ваш форк**:
   ```bash
   git push origin feature/amazing-feature
   ```
7. **Создайте Pull Request** с описанием изменений

## Стандарты кода

### Java Style Guide

- Используйте 4 пробела для отступов
- Имена классов: `PascalCase`
- Имена методов и переменных: `camelCase`
- Константы: `UPPER_SNAKE_CASE`
- Максимальная длина строки: 120 символов

### Пример хорошего кода

```java
/**
 * Пример хорошо документированного класса.
 * 
 * @param <T> тип данных
 */
public class ExampleClass<T> {
    private static final int MAX_SIZE = 100;
    
    /**
     * Выполняет операцию над данными.
     * 
     * @param data входные данные
     * @return результат операции
     * @throws IllegalArgumentException если data равен null
     */
    public T processData(T data) {
        if (data == null) {
            throw new IllegalArgumentException("Data cannot be null");
        }
        // реализация
        return data;
    }
}
```

### Тестирование

- Покрывайте новый код unit-тестами
- Используйте JUnit 5
- Имена тестов должны быть описательными: `testMethodName_condition_expectedResult`
- Используйте Mockito для моков

### Коммиты

Используйте понятные сообщения коммитов:

- ✅ `Add support for custom device drivers`
- ✅ `Fix memory leak in AgentContext`
- ✅ `Update documentation for RemoteServer API`
- ❌ `fix`
- ❌ `update`
- ❌ `changes`

## Процесс ревью

1. Все PR проходят ревью кода
2. Ревьюеры могут запросить изменения
3. После одобрения PR будет объединен в основную ветку

## Вопросы?

Если у вас есть вопросы, создайте issue с меткой "question" или свяжитесь с нами через https://t.me/IoT_tips ).

Спасибо за ваш вклад! 🎉

