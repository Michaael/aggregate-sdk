# Руководство по обработке граничных случаев (Edge Cases)

**Версия:** 1.3.7  
**Дата:** 2024-12-12

---

## 📋 Обзор

Это руководство описывает улучшения обработки граничных случаев (edge cases) в AggreGate SDK для повышения надежности и устойчивости кода.

---

## 🎯 Типы граничных случаев

### 1. Null значения

**Проблема:** Методы могут получать null в качестве параметров, что может привести к `NullPointerException`.

**Решение:**
- Добавить проверки на null в начале методов
- Использовать `Objects.requireNonNull()` для обязательных параметров
- Возвращать безопасные значения по умолчанию для опциональных параметров

**Примеры улучшений:**
- ✅ `Util.getRootCause()` - добавлена проверка на null и защита от циклических ссылок
- ✅ `Util.sortByValue()` - добавлена проверка на null карту и null значения
- ✅ `Util.getIconImages()` - добавлена проверка на null prefix и sizes

---

### 2. Пустые коллекции

**Проблема:** Методы могут получать пустые коллекции, что может привести к ошибкам при итерации.

**Решение:**
- Проверять `isEmpty()` перед итерацией
- Возвращать пустые коллекции вместо null
- Использовать безопасные итераторы

**Примеры улучшений:**
- ✅ `Util.getIconImages()` - возвращает пустой список для null/пустых sizes
- ✅ `Util.sortByValue()` - возвращает пустую карту для null/пустой входной карты

---

### 3. Очень большие значения

**Проблема:** Методы могут получать очень большие значения, что может привести к переполнению или OutOfMemoryError.

**Решение:**
- Добавить проверки на максимальные размеры
- Использовать ограничения (уже реализовано для команд: 100MB-1GB)
- Валидировать размеры перед обработкой

**Уже реализовано:**
- ✅ `AggreGateCommandParser` - ограничение размера команды (100MB-1GB)
- ✅ `BufferedCommandParser` - ограничение размера буфера (1GB+)

---

### 4. Отрицательные значения

**Проблема:** Методы могут получать отрицательные значения там, где ожидаются положительные.

**Решение:**
- Добавить проверки на отрицательные значения
- Использовать `Math.max(0, value)` для защиты
- Валидировать входные параметры

**Примеры улучшений:**
- ✅ `Util.getIconImages()` - пропускает отрицательные размеры иконок

---

### 5. Пустые строки

**Проблема:** Методы могут получать пустые строки, что может привести к ошибкам парсинга.

**Решение:**
- Проверять `StringUtils.isEmpty()` или `str == null || str.isEmpty()`
- Использовать `StringUtils.isBlank()` для проверки на whitespace
- Возвращать безопасные значения по умолчанию

**Уже реализовано:**
- ✅ `ContextUtils.splitPathCached()` - обрабатывает null пути
- ✅ `Util.convertToNumber()` - обрабатывает пустые строки

---

### 6. Циклические ссылки

**Проблема:** Методы, работающие с цепочками объектов, могут попасть в бесконечный цикл при циклических ссылках.

**Решение:**
- Использовать Set для отслеживания посещенных объектов
- Ограничивать глубину рекурсии
- Обнаруживать циклы и прерывать обработку

**Примеры улучшений:**
- ✅ `Util.getRootCause()` - добавлена защита от циклических ссылок в цепочке исключений

---

## 🔍 Критические методы для улучшения

### Уже улучшено (версия 1.3.7)

1. **Util.getRootCause()**
   - ✅ Проверка на null
   - ✅ Защита от циклических ссылок

2. **Util.sortByValue()**
   - ✅ Проверка на null карту
   - ✅ Обработка null значений в карте
   - ✅ Возврат пустой карты для пустой входной карты

3. **Util.getIconImages()**
   - ✅ Проверка на null prefix
   - ✅ Проверка на null/пустую коллекцию sizes
   - ✅ Пропуск null и отрицательных размеров
   - ✅ Обработка ошибок загрузки иконок

4. **AbstractContext.setVariable()**
   - ✅ Улучшена проверка на null oldValue при сравнении значений

5. **ContextUtils.splitPathCached()**
   - ✅ Обработка null путей

---

## 📝 Рекомендации по дальнейшим улучшениям

### Приоритет 1: Критические методы

1. **Методы работы с коллекциями**
   - Проверять на null и пустые коллекции
   - Использовать безопасные итераторы
   - Возвращать пустые коллекции вместо null

2. **Методы парсинга**
   - Проверять на null и пустые строки
   - Обрабатывать невалидные форматы
   - Возвращать понятные сообщения об ошибках

3. **Методы работы с сетью**
   - Обрабатывать разрывы соединения
   - Проверять таймауты
   - Валидировать размеры данных

### Приоритет 2: Важные методы

4. **Методы преобразования типов**
   - Обрабатывать невалидные типы
   - Возвращать безопасные значения по умолчанию
   - Логировать предупреждения

5. **Методы работы с файлами**
   - Проверять существование файлов
   - Обрабатывать ошибки чтения/записи
   - Валидировать размеры файлов

### Приоритет 3: Дополнительные улучшения

6. **Методы работы с потоками**
   - Обрабатывать прерывания
   - Проверять состояние потоков
   - Освобождать ресурсы в finally

---

## ✅ Чеклист для проверки edge cases

При добавлении или изменении метода проверьте:

- [ ] Проверка на null для всех параметров
- [ ] Проверка на пустые коллекции/строки
- [ ] Проверка на отрицательные значения (где применимо)
- [ ] Проверка на очень большие значения (где применимо)
- [ ] Обработка исключений с понятными сообщениями
- [ ] Возврат безопасных значений по умолчанию
- [ ] Логирование предупреждений для неожиданных случаев
- [ ] Тесты для edge cases

---

## 🔗 Связанные документы

- `docs/TROUBLESHOOTING.md` - решение распространенных проблем
- `docs/API_GUIDE.md` - руководство по использованию API
- `СВОДНЫЙ_ПЛАН_УЛУЧШЕНИЙ.md` - план улучшений проекта

---

**Версия:** 1.3.7  
**Дата:** 2024-12-12  
**Статус:** ✅ Завершено

